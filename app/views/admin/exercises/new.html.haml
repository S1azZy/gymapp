- content_for :title, t("admin.exercises.new.title")

%main.admin-page
  .admin-page__header
    .admin-page__heading
      %p.admin-page__eyebrow= t("admin.exercises.eyebrow")
      %h1= t("admin.exercises.new.title")
      %p.admin-page__subtitle= t("admin.exercises.new.subtitle")
  = render "form"

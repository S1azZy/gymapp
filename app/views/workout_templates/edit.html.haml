- content_for :title, t("workout_templates.edit.title")

%main.catalog-page
  .catalog-page__header
    .catalog-page__heading
      %p.catalog-page__eyebrow= t("workout_templates.eyebrow")
      %h1= t("workout_templates.edit.title")
      %p.catalog-page__subtitle= t("workout_templates.edit.subtitle")

  = render "form", workout_template: @workout_template
